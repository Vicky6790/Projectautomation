# Leave empty for local Vite (proxied) and Docker Compose (same-origin nginx).
# On Vercel, set this in Project Settings → Environment Variables before building.
# Example: https://your-api.up.railway.app
VITE_API_BASE=
